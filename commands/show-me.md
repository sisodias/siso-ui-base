# /show-me

Boot the UI Box and hand the human the URL.

`UIBASE_TENANT=<tenant-dir> node app/serve.mjs &`  (default tenant: tenants/oracle, port 8810)
Then tell the human: open http://127.0.0.1:8810/ — grid auto-refreshes as variations land.
